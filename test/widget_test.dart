import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('app smoke test', (tester) async {
    // Minimal placeholder test — full integration tests require network
    expect(true, isTrue);
  });
}
